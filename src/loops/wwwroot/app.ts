for (let i = 0; i < 11; i+=2) {
    console.log(`the loop is currently at itteration ${i}`);
}

let colors: string[] = [
    'red', 'blue', 'green'
];

//Log each value in the colors array

for (i = 0; i < colors.length; i++) {
    console.log(`The loop is at itteration ${i}, and the value of colors[] at that position is ${colors[i]}`);
}